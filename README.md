# synergy-wts CLI tool

The `synergy-wts` CLI tool is the public-facing community version of the Synergy Address Engine.

This tool allows you to create any of the following assets on the Synergy Testnet-beta:

- Wallets
- Fungible tokens
- NFT collections
- Multi-asset token contracts
- Custom contracts
- General multisig identities


## What It Does

The `synergy-wts` CLI tool currently supports:

- Generating community-safe Synergy identities
- Producing public metadata files and encrypted private-key files
- Building full wallet outputs with seed phrases for wallet address types
- Preparing on-chain token and NFT deployment transactions
- Submitting token and NFT deployment transactions over JSON-RPC when a reachable Synergy node is provided
- Decrypting previously generated encrypted key files
- Verifying that an address matches a public key
- Listing supported address types
- Listing supported cryptographic algorithms
- Printing the canonical burn address

All generated private key material is written in encrypted form using the CLI’s hybrid encryption flow.

## Community Address Types

The following address types are open to the community in the `synergy-wts` CLI tool.

| Prefix | Type | Standard | Purpose |
| --- | --- | --- | --- |
| `synw` | Primary Wallet | Wallet | Main user wallet |
| `syns` | Secondary / Utility Wallet | Wallet | Secondary user wallet for operational separation |
| `syna` | Standard Account | Wallet | General-purpose account identity |
| `synz` | UMA Smart Wallet | Wallet | Smart-wallet style account |
| `synb1` | Fungible Token Tier 1 | STS-9 | Community fungible token deployment |
| `synb2` | Fungible Token Tier 2 | STS-9 | Community fungible token deployment |
| `synb3` | Fungible Token Tier 3 | STS-9 | Community fungible token deployment |
| `synn1` | NFT Collection Type 1 | STS-NF | Community NFT collection deployment |
| `synn2` | NFT Collection Type 2 | STS-NF | Community NFT collection deployment |
| `synj` | Multi-Asset Token | STS-MA | Multi-asset game and application contracts |
| `sync` | Custom Contract | Contract | Community-deployed custom contract identity |
| `synm` | General Multisig | Multisig | Shared control identity |

## Installation

Download the release archive that matches your platform:

- `synergy-wts-macos-arm64.tar.gz`
- `synergy-wts-linux-x64.tar.gz`
- `synergy-wts-windows-x64.zip`

### macOS and Linux

```bash
tar xzf synergy-wts-macos-arm64.tar.gz
chmod +x synergy-wts
sudo mv synergy-wts /usr/local/bin/
synergy-wts --help
```

### Windows

1. Extract `synergy-wts.exe` from the release zip.
2. Place it in a directory you control.
3. Add that directory to `PATH`, or run it directly from PowerShell or Command Prompt.

### Verify the Download

Each release archive includes a `.sha256` checksum file.

```bash
shasum -a 256 -c synergy-wts-macos-arm64.tar.gz.sha256
```

### macOS Gatekeeper Note

The public release binaries are not Apple code-signed. On first run, macOS may block the binary. Use one of these methods:

- Right-click `synergy-wts` in Finder and choose `Open`
- Remove the quarantine flag:

```bash
xattr -d com.apple.quarantine /usr/local/bin/synergy-wts
```

- Open it once, then approve it in `System Settings > Privacy & Security`

## Command Overview

```bash
synergy-wts <COMMAND> [OPTIONS]
```

Supported commands:

- `generate <PREFIX>`
- `decrypt <FILE.enc.json> --output <FILE>`
- `verify <ADDRESS> <PUBLIC_KEY_BASE64>`
- `list`
- `algorithms`
- `burn`

## Output Files

Identity generation writes two files:

- `<NAME>.pub.json`
- `<NAME>.enc.json`

### Public File

The public file contains non-secret material such as:

- Address
- Public key
- Address type
- Algorithm
- Creation timestamp
- Wallet metadata for wallet outputs
- Token metadata and on-chain deployment data for token standards

### Encrypted Private File

The encrypted private file contains private key material protected by the CLI’s encryption envelope.

For wallet outputs, the encrypted file also contains the seed phrase.

## Wallet Generation

Wallet address types support either address-only generation or full wallet generation with a seed phrase.

### Address-Only

```bash
synergy-wts generate synw --address-only --output my-primary-wallet
```

### Full Wallet

```bash
synergy-wts generate synw --wallet --seed-words 24 --output my-primary-wallet
```

Wallet-mode output includes:

- Synergy address
- Public key
- Encrypted private key
- Seed phrase
- External address metadata generated by the wallet flow

## Token and NFT Creation

The `synergy-wts` CLI tool supports standards-driven token deployment flows for:

- `STS-9` fungible tokens via `synb1`, `synb2`, `synb3`
- `STS-NF` NFT collections via `synn1`, `synn2`
- `STS-MA` multi-asset contracts via `synj`

For token standards, the CLI now prepares an on-chain deployment transaction and can submit it when a reachable Synergy JSON-RPC endpoint is supplied with `--node`.

### Dry-Run First

Use `--dry-run` to generate the full on-chain artifact set without submitting it:

```bash
synergy-wts generate synb1 \
  --name "Arc Token" \
  --symbol ARC \
  --decimals 9 \
  --max-supply 1000000000 \
  --initial-supply 1000000000 \
  --metadata-uri https://example.com/arc.json \
  --chain-id synergy-testnet-1 \
  --dry-run \
  --output arc-token
```

The public output includes:

- Contract address
- Issuer address
- Canonical metadata hash
- Asset fingerprint
- Deployment transaction payload
- Human-readable deployment manifest
- Signed issuer genesis attestation
- Deployment transaction hash and ID

### Submit to a Node

When you are ready to submit, provide `--node`:

```bash
synergy-wts generate synb1 \
  --name "Arc Token" \
  --symbol ARC \
  --decimals 9 \
  --max-supply 1000000000 \
  --initial-supply 1000000000 \
  --metadata-uri https://example.com/arc.json \
  --chain-id synergy-testnet-1 \
  --network testbeta \
  --node https://rpc.testnet.synergy.network \
  --output arc-token
```

If your node cannot provide a nonce automatically, pass one explicitly:

```bash
synergy-wts generate synb1 \
  --name "Arc Token" \
  --symbol ARC \
  --decimals 9 \
  --max-supply 1000000000 \
  --initial-supply 1000000000 \
  --metadata-uri https://example.com/arc.json \
  --nonce 12 \
  --node https://rpc.testnet.synergy.network \
  --output arc-token
```

## STS-9 Examples

### Fungible Token

```bash
synergy-wts generate synb2 \
  --name "Builder Credit" \
  --symbol BLDR \
  --decimals 9 \
  --max-supply 500000000000 \
  --initial-supply 1000000000 \
  --metadata-uri https://example.com/builder-credit.json \
  --mintable \
  --burnable \
  --output builder-credit
```

Useful STS-9 flags:

- `--name`
- `--symbol`
- `--decimals`
- `--max-supply`
- `--initial-supply`
- `--metadata-uri`
- `--metadata-file`
- `--mintable`
- `--burnable`
- `--pausable`
- `--chain-id`
- `--network`
- `--node`
- `--nonce`
- `--gas-limit`
- `--gas-price-nwei`
- `--dry-run`

## STS-NF Examples

### NFT Collection

```bash
synergy-wts generate synn1 \
  --name "Arc Collection" \
  --symbol ARCX \
  --collection-type standard-transferable \
  --metadata-uri https://example.com/collection.json \
  --token-uri https://example.com/nft/1.json \
  --title "Arc Genesis Collection" \
  --description "Founding collection for Arc" \
  --media-type image/png \
  --dry-run \
  --output arc-collection
```

Useful STS-NF flags:

- `--name`
- `--symbol`
- `--collection-type`
- `--metadata-uri`
- `--metadata-file`
- `--token-uri`
- `--title`
- `--description`
- `--media-type`
- `--chain-id`
- `--network`
- `--node`
- `--nonce`
- `--gas-limit`
- `--gas-price-nwei`
- `--dry-run`

## STS-MA Examples

### Multi-Asset Contract

```bash
synergy-wts generate synj \
  --contract-name "Arc Items" \
  --asset-id sword-001 \
  --asset-type fungible \
  --asset-name "Arc Iron" \
  --asset-symbol AIRON \
  --decimals 0 \
  --max-supply 100000 \
  --initial-supply 5000 \
  --transferability transferable \
  --metadata-uri https://example.com/arc-items.json \
  --mintable \
  --output arc-items
```

Useful STS-MA flags:

- `--contract-name`
- `--name`
- `--asset-id`
- `--asset-type`
- `--asset-name`
- `--asset-symbol`
- `--decimals`
- `--max-supply`
- `--initial-supply`
- `--transferability`
- `--metadata-uri`
- `--metadata-file`
- `--mintable`
- `--burnable`
- `--chain-id`
- `--network`
- `--node`
- `--nonce`
- `--gas-limit`
- `--gas-price-nwei`
- `--dry-run`

## Custom Contracts

Generate a custom contract identity:

```bash
synergy-wts generate sync --output my-contract
```

This generates the contract identity artifacts only. Runtime logic and deployment flow remain application-specific.

## General Multisig

Generate a multisig identity:

```bash
synergy-wts generate synm --output team-multisig
```

This generates the multisig identity artifacts only. It does not configure signer thresholds or a separate governance runtime by itself.

## Decrypting Encrypted Output

To decrypt an encrypted private file:

```bash
synergy-wts decrypt arc-token.enc.json --output arc-token.private.json
```

You can also set the passphrase non-interactively:

```bash
export SYNERGY_DECRYPT_PASSPHRASE='your-passphrase'
synergy-wts decrypt arc-token.enc.json --output arc-token.private.json
```

## Verifying an Address

```bash
synergy-wts verify synw1... <BASE64_PUBLIC_KEY>
```

This checks whether the address matches the provided public key.

## Supported Algorithms

The `synergy-wts` CLI tool supports the same algorithm families exposed by the engine for community-safe generation:

- `ml-dsa-44`
- `ml-dsa-65`
- `ml-dsa-87`
- `fn-dsa-512`
- `fn-dsa-1024`
- `ml-kem-512`
- `ml-kem-768`
- `ml-kem-1024`
- `hqc-128`
- `hqc-192`
- `hqc-256`
- `ed25519`

For on-chain token deployment, use a signature algorithm. KEM algorithms are not valid for signing deployment transactions.

## Environment Variables

- `SYNERGY_PASSPHRASE`
- `SYNERGY_DECRYPT_PASSPHRASE`

These allow non-interactive encryption and decryption flows.

## Security Notes

- Keep `.enc.json` files and passphrases separate
- Treat decrypted private-key files as highly sensitive
- Use `--dry-run` first when preparing a token or NFT deployment
- Preserve the generated public file for auditability and later verification
- Unverified token outputs should be treated as unverified until accepted by the canonical registry flow

## Quick Start

Generate a wallet:

```bash
synergy-wts generate synw --wallet --output my-wallet
```

Prepare a fungible token deployment:

```bash
synergy-wts generate synb1 \
  --name "My Token" \
  --symbol MYT \
  --decimals 9 \
  --max-supply 1000000000 \
  --initial-supply 1000000 \
  --metadata-uri https://example.com/my-token.json \
  --dry-run \
  --output my-token
```

List available community prefixes:

```bash
synergy-wts list
```
